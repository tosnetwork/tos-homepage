# Backup files
*.backup
*.old
*.bak

# Development files
check-links.html
test-checklist.md

# OS files
.DS_Store
Thumbs.db

# Editor files
.vscode/
.idea/
*.swp
*.swo

# Node modules (if added later)
node_modules/
package-lock.json

# Build files (if added later)
dist/
build/
.wrangler/

# Private material that must never reach a public repository or a deploy.
# Note that wrangler does not read this file: `wrangler pages deploy .` would
# still upload it. Deploy from `git archive HEAD` instead.
*.pptx
